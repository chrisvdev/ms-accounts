// usas la contraseña con un max de 24 char, usas los ultimos 12 char del hash de la fecha y hora

/* import secrets
 def generar_sal(): return secrets.token_hex(16) */

import * as crypto from "crypto";
function generarSal(): string {}
